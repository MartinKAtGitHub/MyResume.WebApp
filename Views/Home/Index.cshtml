@model UserSearchViewModel




<div id="homePageLogoContainer">


</div>

<div>
    <form asp-controller="Home" asp-action="Index" method="get">
        <div class="input-group">
            <input class="form-control " type="search" placeholder="Search" asp-for="SearchString">
            <div class="input-group-append"><button type="submit" class="btn btn-primary">Search</button></div>
        </div>
    </form>
</div>


<div id="searchResultContainer">
    @*<ul>
            @foreach (var User in Model.UsersResult)
            {
                <li>
                    <a asp-controller="Home" asp-action="UserResume" asp-route-id="@User.Id">@User.UserName</a>
                </li>
            }
        </ul>*@



    @foreach (var User in Model.UsersResult)
    {

        var avatarImgPath = "~/images/AvatarImages/" + (User.AvatarImgPath ?? "../MyResumeIcon.png");

        <div id="searchResultElement">

            <div class="elementColumn"><img id="searchResultAvatarImg" class="img-fluid" src="@avatarImgPath" alt="Avatar_@User.ApplicationUser.UserName" asp-append-version="true" /></div>

            <div id="nameContainer">
                <div class="elementColumn" id="searchResultFirstName">@User.FirstName</div>
                <div class="elementColumn" id="searchResultMiddelName">@User.MiddelName</div>
                <div class="elementColumn" id="searchResultLastName">@User.LasttName</div>
            </div>

            <div class="elementColumn" id="searchResultUserName">( @User.ApplicationUser.UserName )</div>


            <div class="elementColumn"><a asp-controller="Home" asp-action="UserResume" asp-route-id="@User.ApplicationUser.Id">Resume</a></div>
            <div class="elementColumn"> <a herf="#">Contact</a></div>
        </div>
    }

</div>



